/**
 * KPI labels for the workspace overview (Horizon-style dashboard).
 * Order matches API fields: total feedbacks, rating, top source category, sentiment.
 */
export const workspaceDashboardKpiCopy = {
  totalFeedbacks: "Total Feedbacks",
  averageRating: "Average Rating",
  topCategory: "Top Category",
  positiveSentiment: "Positive Sentiment",
} as const;
